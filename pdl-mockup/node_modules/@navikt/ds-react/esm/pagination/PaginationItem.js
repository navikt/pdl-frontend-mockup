var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from "react";
import { Button } from "../button/index.js";
import { useRenameCSS, useThemeInternal } from "../theme/Theme.js";
export const Item = forwardRef((_a, ref) => {
    var { children, as: Component = "button", selected = false, className, page } = _a, rest = __rest(_a, ["children", "as", "selected", "className", "page"]);
    const themeContext = useThemeInternal(false);
    const { cn } = useRenameCSS();
    return (React.createElement(Button, Object.assign({ as: Component, variant: themeContext ? "tertiary-neutral" : "tertiary", "aria-current": selected, "data-selected": selected, ref: ref, className: cn("navds-pagination__item", className, {
            "navds-pagination__item--selected": selected,
        }), "data-page": page, 
        /* TODO: Breaking change to remove since it's in use by some applications. Add to future major version. */
        page: page }, (Component === "button" && { type: "button" }), rest), children));
});
export default Item;
//# sourceMappingURL=PaginationItem.js.map